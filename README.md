# Детекция логотипа Т-Банка.

Решение для кейса CV "Детекция логотипа Т-Банка" 
**Автор**: Анна Губина
**Дата**: Сентябрь 2025  

---

## Цель кейса

Создать REST API сервис, который:
- Обнаруживает логотип Т-Банка (стилизованная буква "Т" в щите) на изображениях.
- Игнорирует логотипы "Тинькофф", "Роснефть" и другие посторонние объекты.
- Возвращает координаты найденных логотипов в формате `BoundingBox`.
- Работает **не более чем 10 секунд на изображение**.
- Поддерживает форматы: JPEG, PNG, BMP, WEBP.
- Доступен по порту 8000.

API соответствует контракту и упакован в Docker-контейнер.

---

##  Пайплайн решения

### 2. Этап 1: Создание датасета только с логотипами Т-Банка.

Из данного в задании датасета (30к фотографий) я вручную выбрала 600 фото без лого других компаний. 
Это сделано с целью оптимизации автоматической разметки. Запуск на всей папке занял бы очень много времени.


### 2. Этап 2: Grounding DINO — генерация кандидатов

Использована модель **`IDEA-Research/grounding-dino-base`** — современная архитектура для zero-shot детекции, сочетающая DINO и текстовый энкодер.

- **Промт**: `"shield-formed logo"`  
  (выбран после экспериментов: короткие промты дают лучший результат).
- **Порог уверенности**: `0.35`.
- **Результат**: 539 изображений с кандидатами на логотип.

  

>  Официальная документация: [Hugging Face — Grounding DINO](https://huggingface.co/docs/transformers/model_doc/grounding-dino)

---

### 3. Этап 3: Фильтрация по геометрии и цвету

После DINO применены **два уровня фильтрации**:

####  Геометрия:
- Отношение сторон:  
  - `0.6 ≤ aspect ≤ 1.5` — для стандартных объектов.  
  - `0.8 ≤ aspect ≤ 1.25` — для крупных объектов (>25% площади изображения).
- Минимальная площадь: `0.04%` от общего размера.
- Максимальная площадь: `25%` — исключает фоновые элементы.

####  Цвет (функция `is_shield_color()`):
Анализируется цвет центральной части обрезанного бокса:
- **Светлый щит**: `R, G > 160`, разница между каналами < 40.
- **Жёлтый щит**: `R, G высокие`, `B < R - 50`.
- **Тёмный щит**: `R, G, B < 50`, разница < 30.

**Результат**: 738 боксов прошли фильтрацию.

---

### 4. Этап 4: CLIP — финальная валидация

Использован **CLIP (ViT-B/32)** :
- **Позитивные эталоны**: 16 изображений логотипа Т-Банка (разные ракурсы, цвета).
- **Негативные эталоны**: 38 логотипов "Тинькофф", "Роснефть", случайных объектов и так далее.
- **Порог схожести**: `0.25` — если `sim_pos > sim_neg` и `sim_pos > 0.25`, бокс сохраняется.

> Прошли фильтр: 558 из 738 детекций.

**Результат**: 484 фотографии с подтверждёнными логотипами.

---

### 5. Этап 5: Ручная проверка и постобработка

Хотя разметка была автоматической, **все изображения, прошедшие CLIP, были визуально проверены**:

- Удалены оставшиеся ложные срабатывания.
- Добавлены отсутсвующие bboxы.
- Так же было добавлено около 390 негативных примеров из начального датасета (30к фото).

>  Это обеспечило **высокое качество разметки**, несмотря на автоматический пайплайн.

---


### 6. Этап 6: Аугментация

Были применены следующие шаги аугментации данных:

- Поворот (Rotate): изображения могут поворачиваться на 90° по часовой стрелке или против.

- Обрезка (Crop): случайное кадрирование с масштабированием — минимум 0% (без зума), максимум 12% увеличения.

- Сдвиг/срез (Shear): искажение изображения под углом — до ±13° по горизонтали и до ±9° по вертикали.

- Размытие (Blur): добавляется размытие до 1.8 пикселя.

- Шум (Noise): добавляется шум до 1.48% пикселей изображения.

- В сумме вышло 2287 изображений, что достаточно для обучения модели детекции для нашей задачи.

https://app.roboflow.com/training-model-oprwl/t-bank-logo-labeling-p93h4/2 - датасет



### 7. Создание датасета YOLO

На основе отфильтрованных данных создан датасет в формате YOLOv8:
- Координаты: `(xc, yc, w, h)` — нормализованы по размеру изображения.
- Класс: `T-Bank logo` (ID: 0).
- Все изображения сохранены в `yolo_dataset/images/`, разметка — в `yolo_dataset/labels/`.

**Разделение**:
- **Train**: 2112 фото с разметкой, 936 без
- **Valid**: 175 фото с разметкой, 78 без
---

### 8. Обучение модели

Использована **YOLOv8s** — оптимальный выбор по точности/скорости.

**Параметры обучения**:
- Размер входа: `640x640`.
- Epochs: `30`.
- Batch: `16`.
- Порог уверенности: `0.4`.
- Ранняя остановка: `patience=5` (чтобы избежать переобучения).
- Оптимизатор: SGD с momentum.
- Learning rate: 0.01.

**Результат**:
- Обучение завершено за ~15-20 минут на GPU T100.
- Модель сохранена: `weights/best.pt`.

---

### 9. Метрики качества

Оценка на валидационной выборке (IoU=0.5):

 Precision: 0.94
 Recall:    0.98
 F1-score:  0.96


>  Высокие метрики объясняются качеством разметки и хорошей обобщающей способностью.

---

### 10. Примеры детекции

![Примеры детекции на валидационной выборке](collage.png)

---

### 11. Время обработки

- Укладывается в требование: **<10 секунд**.

---

## Источник разработки

Полный процесс разметки, обучения и валидации проводился в Kaggle:  
 [Ссылка на Kaggle Notebook](https://www.kaggle.com/code/x05su38q/t-bank-logo-detection)

---

## 🐳 Как запустить (Docker)

### Требования
- Установленный [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Включённая виртуализация в BIOS
- Windows 10/11 или актуальная версия WSL 2

---

###  Шаг 1: Установите Docker Desktop

1. Перейдите на [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
2. Скачайте и установите Docker Desktop.
3. Запустите Docker.
---

###  Шаг 2: Откройте терминал в папке проекта

1. Откройте командную строку
2. Перейдите в проводнике Windows в папку с проектом.

### Шаг 3: Соберите Docker-образ

- docker build -t tbank-detection .

После этого должна начаться сборка и установка зависимостей, прописаннных в файле requirements.txt

### Шаг 4: Запусти контейнер

- docker run -p 8000:8000 tbank-detection

docker run — запуск контейнера.
-p 8000:8000 — проброс порта: внешний 8000 → внутренний 8000.
tbank-detection — имя собранного образа

### Шаг 5: Проверьте работу API

Откройте браузер и перейди по ссылке:

 http://localhost:8000/docs

 Вы должны увидеть Swagger UI — интерфейс для тестирования API

 ### Шаг 6: Протестируйте детекцию

1. На странице найдите эндпоинт /detect.
2. Нажмите "Try it out".
3. Нажмите "Choose File" и выберите любое изображение с логотипом Т-Банка.
4. Нажмите "Execute".

Ниже будет показвн результат

### Шаг 7: Как остановить сервер

В терминале, где работает docker run, нажмиnt CTRL + C


## Предложения альтернативных подходов к решению
В качестве альтернативного подхода можно было взять часть из датасета, например 5000 фото, включая позитивные и негативные примеры. А далее идти по задуманному пайплайну: Grounding Dino + пороги + Clip. Тогда использование CLIP оправдало бы себя, так как мы бы явно отсеивали положительные и негативные примеры. С другой стороны, это вероятнее всего увеличило бы время ручной проверки меток, так как данных бы было больше, а также ошибок. Я считаю, что в данной задаче детектируемые объекты не очень сложные, поэтому достаточно небольшого датасета с добавлением аугментаций.

## Проведенный анализ производительности решения и предложение вариантов по его ускорению
Приведенное решение обладает достаточной производительностью, ведь мы существенно уменьшаем время, потраченное на разметку, используя zero-shot и few-shot подходы.

## Проведенный анализ проблем решения и рассуждения по возможности их решения
Если вдруг по какой-то причине среда разработки прервется, придется заново запускать код автоматической разметки, так как сейчас там не реализована логика с чекпоинтами. В моем случае так было несколько раз, но из-за того, что датасет небольшой (всего было взято 600 фото), это занимет не слишклм много времени. 
